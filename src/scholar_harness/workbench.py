# ruff: noqa: E501

WORKBENCH_HTML = """<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1">
  <meta http-equiv="Content-Security-Policy" content="default-src 'self'; style-src 'unsafe-inline'; script-src 'unsafe-inline'; connect-src 'self' ws: wss:">
  <title>ScholarHarness · Workbench</title>
  <style>
    :root { color-scheme: dark; --ink:#101515; --deep:#151d1c; --panel:#1b2523; --panel2:#222e2b; --line:#34423e; --paper:#e8eadf; --muted:#91a09a; --cyan:#6ee7dd; --lime:#c5f467; --amber:#f4c56b; --red:#ff817a; --shadow:0 24px 70px #0005; font-family:Inter,ui-sans-serif,system-ui,-apple-system,sans-serif; }
    *{box-sizing:border-box} body{margin:0;background:var(--ink);color:var(--paper);min-height:100vh} body:before{content:"";position:fixed;inset:0;pointer-events:none;opacity:.25;background-image:linear-gradient(#6ee7dd09 1px,transparent 1px),linear-gradient(90deg,#6ee7dd09 1px,transparent 1px);background-size:38px 38px;mask-image:linear-gradient(#000,transparent 78%)}
    button,input,select,textarea{font:inherit} button:focus-visible,input:focus-visible,select:focus-visible,textarea:focus-visible,a:focus-visible{outline:3px solid var(--cyan);outline-offset:3px}.shell{display:grid;grid-template-columns:250px minmax(0,1fr);min-height:100vh}.rail{position:sticky;top:0;height:100vh;padding:30px 22px;border-right:1px solid var(--line);background:#101515eb;backdrop-filter:blur(18px);z-index:2}.brand{display:flex;gap:12px;align-items:center;margin-bottom:40px}.brand-mark{display:grid;place-items:center;width:42px;height:42px;border:1px solid var(--cyan);border-radius:13px;color:var(--cyan);font:700 15px ui-monospace,monospace}.brand strong{display:block}.brand small,.muted{color:var(--muted)}nav{display:grid;gap:8px}.nav-button{border:0;background:transparent;color:var(--muted);text-align:left;padding:12px 14px;border-radius:11px;cursor:pointer}.nav-button:hover{color:var(--paper);background:var(--panel)}.nav-button[aria-current=page]{color:var(--ink);background:var(--lime);font-weight:700}.rail-foot{position:absolute;left:22px;right:22px;bottom:24px;color:var(--muted);font-size:12px;line-height:1.6}.rail-foot a{color:var(--cyan)}
    main{min-width:0;padding:34px clamp(22px,4vw,62px) 70px}.topbar{display:flex;justify-content:space-between;gap:22px;align-items:flex-end;margin-bottom:32px}.eyebrow{color:var(--cyan);text-transform:uppercase;letter-spacing:.16em;font:700 11px ui-monospace,monospace}h1{font-size:clamp(28px,4vw,48px);letter-spacing:-.045em;line-height:1;margin:9px 0 0}h2{margin:0;font-size:22px}h3{margin:0;font-size:15px}.service-state{display:flex;gap:9px;align-items:center;padding:9px 12px;background:var(--panel);border:1px solid var(--line);border-radius:999px;color:var(--muted);font-size:12px}.service-state i{width:8px;height:8px;border-radius:50%;background:var(--lime);box-shadow:0 0 14px var(--lime)}.section-head{display:flex;justify-content:space-between;gap:18px;align-items:center;margin-bottom:18px}.section-head p{margin:5px 0 0;color:var(--muted);font-size:13px}
    .metrics{display:grid;grid-template-columns:repeat(4,minmax(120px,1fr));gap:12px;margin-bottom:26px}.metric,.card{background:linear-gradient(145deg,#222e2bf0,#192220f0);border:1px solid var(--line);border-radius:18px;box-shadow:var(--shadow)}.metric{min-height:126px;padding:18px;display:flex;flex-direction:column;justify-content:space-between}.metric span{color:var(--muted);font-size:12px}.metric strong{font:650 34px ui-monospace,monospace;color:var(--paper)}.card{padding:20px}.split{display:grid;grid-template-columns:minmax(260px,.75fr) minmax(420px,1.6fr);gap:16px;align-items:start}.stack{display:grid;gap:10px}.list-button{width:100%;border:1px solid var(--line);background:var(--deep);color:var(--paper);padding:13px;border-radius:12px;text-align:left;cursor:pointer}.list-button:hover,.list-button.selected{border-color:var(--cyan);background:#6ee7dd14}.list-button small{color:var(--muted);display:block;margin-top:6px}.row{display:flex;justify-content:space-between;gap:14px;align-items:baseline}.mono{font-family:ui-monospace,SFMono-Regular,Menlo,monospace}.pill{display:inline-flex;padding:4px 8px;border-radius:999px;border:1px solid var(--line);color:var(--muted);font:700 10px ui-monospace,monospace;text-transform:uppercase}.pill.completed,.pill.confirmed,.pill.passed{color:var(--lime)}.pill.running,.pill.candidate{color:var(--cyan)}.pill.failed,.pill.rejected,.pill.regression{color:var(--red)}.pill.aborted,.pill.superseded{color:var(--amber)}
    .timeline{border-left:1px solid var(--line);margin-left:7px;padding-left:20px;display:grid;gap:12px}.timeline-item{position:relative;padding:14px;border-radius:12px;background:var(--deep);border:1px solid var(--line)}.timeline-item:before{content:"";position:absolute;left:-25px;top:19px;width:8px;height:8px;border-radius:50%;background:var(--cyan)}pre{white-space:pre-wrap;overflow-wrap:anywhere;margin:12px 0 0;padding:12px;background:#0d1212;border-radius:9px;color:#b9c9c3;font:11px/1.55 ui-monospace,monospace;max-height:300px;overflow:auto}.detail-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:9px;margin:14px 0 20px}.detail-grid div{background:var(--deep);padding:10px;border-radius:9px;min-width:0}.detail-grid dt{color:var(--muted);font-size:10px;text-transform:uppercase}.detail-grid dd{margin:5px 0 0;overflow-wrap:anywhere;font:12px ui-monospace,monospace}.toolbar{display:flex;gap:9px;align-items:center;flex-wrap:wrap}input,select{background:var(--deep);color:var(--paper);border:1px solid var(--line);border-radius:10px;padding:10px 12px}input{min-width:min(320px,100%)}.action{border:1px solid var(--line);border-radius:10px;padding:10px 13px;background:var(--panel2);color:var(--paper);cursor:pointer}.action:hover{border-color:var(--cyan)}.action.primary{color:var(--ink);background:var(--lime);font-weight:700}.action.danger{color:var(--red)}.action:disabled{opacity:.5;cursor:wait}
    .memory-card,.paper-card,.result-card,.tool-card{border:1px solid var(--line);background:var(--deep);border-radius:13px;padding:15px}.memory-card p,.result-card p{line-height:1.6;color:#c9d1cc}blockquote{margin:12px 0;border-left:2px solid var(--amber);padding-left:12px;color:var(--muted);font-size:12px}.state{border:1px dashed var(--line);border-radius:13px;padding:20px;color:var(--muted);text-align:center}.state[data-kind=error]{color:var(--red)}.state[data-kind=loading]{color:var(--cyan)}.subhead{margin:24px 0 12px;color:var(--muted);font-size:12px;text-transform:uppercase;letter-spacing:.1em}[hidden]{display:none!important}
    .chat-layout{display:grid;grid-template-columns:minmax(0,1.55fr) minmax(280px,.65fr);gap:16px;align-items:start}.chat-log{min-height:380px;max-height:58vh;overflow:auto;display:flex;flex-direction:column;gap:12px;padding:4px}.chat-message{max-width:86%;padding:13px 15px;border-radius:16px;white-space:pre-wrap;overflow-wrap:anywhere;line-height:1.55}.chat-message.user{align-self:flex-end;background:var(--lime);color:var(--ink);border-bottom-right-radius:4px}.chat-message.assistant{align-self:flex-start;background:var(--deep);border:1px solid var(--line);border-bottom-left-radius:4px}.chat-activity{max-height:58vh;overflow:auto}.activity-item{padding:10px 0;border-bottom:1px solid var(--line);font-size:12px;overflow-wrap:anywhere}.activity-item strong{color:var(--cyan)}.chat-compose{display:grid;gap:10px;margin-top:16px}.chat-compose textarea{width:100%;min-height:96px;resize:vertical;background:var(--deep);color:var(--paper);border:1px solid var(--line);border-radius:12px;padding:12px}.session-id{max-width:420px;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.compact-input{min-width:220px;flex:1}
    .eval-layout{display:grid;grid-template-columns:minmax(300px,.8fr) minmax(440px,1.4fr);gap:16px;align-items:start}.form-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:12px}.form-grid label{display:grid;gap:6px;color:var(--muted);font-size:12px}.form-grid .wide{grid-column:1/-1}.form-grid input,.form-grid select,.form-grid textarea{width:100%;min-width:0}.form-grid textarea{min-height:92px;resize:vertical;background:var(--deep);color:var(--paper);border:1px solid var(--line);border-radius:10px;padding:10px 12px}.check-label{display:flex!important;grid-column:1/-1;align-items:center;grid-template-columns:auto 1fr!important}.check-label input{width:auto}.history-row{display:grid;grid-template-columns:130px minmax(100px,1fr) 90px;gap:10px;align-items:center;font-size:12px}.score-track{height:9px;background:#0d1212;border-radius:99px;overflow:hidden}.score-bar{height:100%;background:var(--cyan);border-radius:99px}.history-row.regression .score-bar{background:var(--red)}.check-card{border:1px solid var(--line);background:var(--deep);border-radius:13px;padding:14px}.check-evidence{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:10px}.check-evidence pre{margin-top:7px}.editor-mode{color:var(--cyan);font:700 11px ui-monospace,monospace;text-transform:uppercase}.guide-hero{padding:28px;margin-bottom:16px;background:linear-gradient(135deg,#21332f,#1b2523 62%,#26331b);border:1px solid var(--line);border-radius:20px}.guide-hero p{max-width:780px;color:#c9d1cc;line-height:1.75}.guide-flow{display:grid;grid-template-columns:repeat(5,1fr);gap:10px;margin:18px 0}.guide-step{position:relative;padding:16px 14px;background:var(--deep);border:1px solid var(--line);border-radius:13px}.guide-step b{display:block;color:var(--lime);font:700 12px ui-monospace,monospace;margin-bottom:8px}.guide-step span{font-size:13px;line-height:1.5}.guide-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:14px}.guide-card{padding:20px;background:linear-gradient(145deg,#222e2bf0,#192220f0);border:1px solid var(--line);border-radius:16px}.guide-card p,.guide-card li{color:#c9d1cc;line-height:1.7}.guide-card ol,.guide-card ul{padding-left:20px;margin-bottom:0}.guide-card code{color:var(--cyan)}
    .graph-scroll{position:relative;overflow:hidden;height:430px;min-height:260px;margin:14px 0;border:1px solid var(--line);border-radius:14px;background:#111b19;cursor:grab;touch-action:none}.graph-scroll.panning{cursor:grabbing}.graph-canvas{position:absolute;left:0;top:0;transform-origin:0 0}.graph-edges{position:absolute;inset:0;overflow:visible;pointer-events:none}.graph-node{position:absolute;width:224px;height:102px;overflow:hidden;padding:12px;text-align:left;border:1px solid var(--line);border-radius:12px;background:var(--deep);color:var(--paper);cursor:grab;touch-action:none;user-select:none}.graph-node.dragging{cursor:grabbing;z-index:1}.graph-node:hover,.graph-node.selected{border-color:var(--cyan);background:#213a35}.graph-node.active{box-shadow:inset 3px 0 var(--lime)}.graph-node small{display:block;color:var(--muted);margin:5px 0;overflow:hidden;text-overflow:ellipsis;white-space:nowrap}.graph-actions,.graph-viewport-actions{display:flex;gap:9px;flex-wrap:wrap;align-items:center}.graph-viewport-actions{margin-top:12px}.graph-zoom{min-width:54px;text-align:center;color:var(--muted);font:700 12px ui-monospace,monospace}.graph-inspector{border-top:1px solid var(--line);margin-top:14px;padding-top:14px}.graph-caption{color:var(--muted);line-height:1.6;font-size:13px}
    @media(max-width:1080px){.metrics{grid-template-columns:repeat(3,1fr)}.split,.chat-layout,.eval-layout{grid-template-columns:1fr}.guide-flow{grid-template-columns:repeat(3,1fr)}}@media(max-width:820px){.shell{display:block}.rail{position:relative;height:auto;padding:18px;border-right:0;border-bottom:1px solid var(--line)}.brand{margin-bottom:16px}nav{grid-template-columns:repeat(3,1fr)}.nav-button{text-align:center;padding:10px 6px}.rail-foot{display:none}main{padding:24px 16px 50px}.metrics{grid-template-columns:repeat(2,1fr)}.guide-grid{grid-template-columns:1fr}}@media(max-width:520px){nav{grid-template-columns:repeat(2,1fr)}.topbar{display:block}.service-state{margin-top:18px;width:fit-content}.detail-grid,.form-grid,.check-evidence,.guide-flow{grid-template-columns:1fr}.form-grid .wide{grid-column:auto}.history-row{grid-template-columns:1fr}.chat-message{max-width:96%}}@media(prefers-reduced-motion:reduce){*{transition:none!important}}
  </style>
</head>
<body><div class="shell">
  <aside class="rail"><div class="brand"><div class="brand-mark">SH</div><div><strong>ScholarHarness</strong><small>个人文献 Agent 实验室</small></div></div><nav aria-label="工作台导航"><button class="nav-button" data-route="overview" aria-current="page">总览</button><button class="nav-button" data-route="guide">使用指南</button><button class="nav-button" data-route="chat">Agent 对话</button><button class="nav-button" data-route="library">文献库</button><button class="nav-button" data-route="memories">记忆审核</button><button class="nav-button" data-route="runs">运行追踪</button><button class="nav-button" data-route="evaluations">评测实验室</button></nav><div class="rail-foot">本地可观测工作台<br><a href="/docs">OpenAPI 文档</a> · <a href="/health">健康状态</a></div></aside>
  <main><header class="topbar"><div><div class="eyebrow">让 Agent 的每一步都可观测</div><h1 id="page-title">系统总览</h1></div><div class="service-state"><i></i><span id="service-label">本地服务已连接</span></div></header>
    <section data-page="overview"><div class="metrics"><article class="metric"><span>Agent 运行数</span><strong id="metric-runs">—</strong></article><article class="metric"><span>工具调用数</span><strong id="metric-tools">—</strong></article><article class="metric"><span>候选记忆</span><strong id="metric-candidates">—</strong></article><article class="metric"><span>已确认记忆</span><strong id="metric-confirmed">—</strong></article><article class="metric"><span>文献数</span><strong id="metric-papers">—</strong></article><article class="metric"><span>评测结果</span><strong id="metric-evals">—</strong></article><article class="metric"><span>通过的评测</span><strong id="metric-eval-passed">—</strong></article><article class="metric"><span>回归问题</span><strong id="metric-regressions">—</strong></article></div><article class="card"><div class="section-head"><div><h2>最近运行</h2><p>从不同 Runtime 统一投影的可观测 Trace</p></div><button class="action" data-go="runs">查看全部</button></div><div class="state" data-state="overview">等待加载</div><div class="stack" id="recent-runs"></div></article></section>
    <section data-page="guide" hidden><div class="guide-hero"><div class="eyebrow">第一次使用</div><h2>从一篇论文到一个可验证的 Agent 结论</h2><p>ScholarHarness 不只是聊天页面，它是用来学习和展示 Memory、Tool Calling、Trace 与 Evaluation 的完整 Harness。按下面路线操作，你可以看到模型如何找证据、调工具、形成记忆，以及如何防止回归。</p><div class="guide-flow"><div class="guide-step"><b>01 导入</b><span>将 PDF 放入文献库</span></div><div class="guide-step"><b>02 对话</b><span>让 Agent 检索并回答</span></div><div class="guide-step"><b>03 观测</b><span>检查 Trace 与 Tool Calling</span></div><div class="guide-step"><b>04 审核</b><span>确认有证据的 Memory</span></div><div class="guide-step"><b>05 评测</b><span>用 Case / Suite 防止回归</span></div></div><button class="action primary" data-go="library">从文献库开始</button></div><div class="guide-grid"><article class="guide-card"><h3>文献库：Agent 的证据来源</h3><ol><li>通过 <a href="/docs">OpenAPI</a> 的 <code>POST /papers/import/pdf</code> 上传可选中文字的 PDF。</li><li>回到“文献库”刷新目录，输入关键词进行混合检索。</li><li>结果会保留 paper / passage / page 坐标，便于引用验证。</li></ol></article><article class="guide-card"><h3>Agent 对话：运行 Python Harness</h3><ol><li>点击“新建会话”，创建独立 MiniPy Runtime 会话。</li><li>给出明确任务，例如“检索文献并用原文证据比较两种 Memory 方法”。</li><li>右侧实时显示 Runtime 事件、上下文注入和工具调用。</li></ol></article><article class="guide-card"><h3>记忆审核：人在回路的信任边界</h3><p>Agent 用 <code>save_memory</code> 提交的内容先进入 <strong>candidate（候选）</strong>，不会立即影响以后对话。你核对原文证据后点击“确认”，它才成为 <strong>confirmed（可信）</strong> 上下文；错误内容应拒绝。</p></article><article class="guide-card"><h3>运行追踪：打开 Agent 黑盒</h3><p>每轮对话都形成 Run。这里可查看事件时间线、Tool Calling 入参与返回值、耗时、错误和 Runtime 类型。面试展示时，用它解释“Agent 为什么得到这个答案”。</p></article><article class="guide-card"><h3>评测实验室：把预期写成可执行规则</h3><p><strong>Case</strong> 定义 Prompt 和确定性检查，例如必须调用 <code>search_papers</code>、禁止某工具、必须验证引用。<strong>Suite</strong> 按顺序组合多个 Case，适合做回归测试和 CI 质量门。</p></article><article class="guide-card"><h3>建议的第一个实验</h3><p>导入一篇 Agent Memory 论文 → 让 Agent 检索并总结 → 在运行追踪中确认它真的调用了检索工具 → 审核它保存的记忆 → 创建一个“必须检索且验证引用”的 Case。</p></article></div></section>
    <section data-page="runs" hidden><div class="section-head"><div><h2>运行追踪</h2><p>事件时间线与关联的 Tool Calling</p></div><button class="action" id="refresh-runs">刷新</button></div><div class="split"><article class="card"><div class="state" data-state="runs">等待加载</div><div class="stack" id="runs-list"></div></article><article class="card" id="run-detail"><div class="state" data-state="run-detail">选择一条运行</div></article></div></section>
    <section data-page="memories" hidden><div class="section-head"><div><h2>记忆审核</h2><p>Agent 提出 candidate；你决定可信上下文</p></div><div class="toolbar"><label for="memory-filter">状态</label><select id="memory-filter"><option value="candidate">候选（candidate）</option><option value="confirmed">已确认（confirmed）</option><option value="rejected">已拒绝（rejected）</option><option value="superseded">已替代（superseded）</option><option value="">全部</option></select><button class="action" id="refresh-memories">刷新</button></div></div><div class="state" data-state="memories">等待加载</div><div class="stack" id="memory-list"></div></section>
    <section data-page="library" hidden><div class="section-head"><div><h2>文献库</h2><p>检索结果保留文献、片段与页码坐标</p></div><button class="action" id="refresh-library">刷新目录</button></div><article class="card"><form class="toolbar" id="search-form"><label for="search-query">检索</label><input id="search-query" required placeholder="例如：Memory 来源、Tool Calling…"><label for="search-mode">模式</label><select id="search-mode"><option value="hybrid">混合检索（hybrid）</option><option value="lexical">关键词检索（lexical）</option></select><button class="action primary" type="submit">搜索文献片段</button></form><div class="state" data-state="search" hidden></div><div class="stack" id="search-results"></div></article><div class="subhead">文献目录</div><div class="state" data-state="library">等待加载</div><div class="stack" id="paper-list"></div></section>
    <section data-page="chat" hidden><div class="section-head"><div><h2>Agent 对话</h2><p>MiniPy Runtime · 服务端模型 · 实时标准化事件</p></div><div class="toolbar"><button class="action primary" id="new-chat">新建会话</button><button class="action" id="reconnect-chat">重连</button><button class="action danger" id="delete-chat">删除</button></div></div><div class="state" data-state="chat">尚未连接会话</div><div class="row"><span class="mono muted session-id" id="chat-session-id">会话 —</span><span class="pill" id="chat-connection">未连接</span></div><div class="chat-layout"><article class="card"><div class="chat-log" id="chat-log" aria-live="polite"></div><form class="chat-compose" id="chat-form"><label for="chat-prompt">给研究 Agent 的任务</label><textarea id="chat-prompt" required placeholder="例如：检索文献库并总结 Tool Calling 的可靠性证据"></textarea><div class="toolbar"><button class="action primary" id="send-chat" type="submit">发送</button><button class="action danger" id="abort-chat" type="button">中止当前轮</button></div></form></article><aside class="card"><div class="row"><h3>Runtime 实时活动</h3><span class="pill" id="chat-busy">空闲</span></div><div class="stack chat-activity" id="chat-activity" aria-live="polite"></div><div class="subhead">上下文控制</div><div class="stack"><label for="compact-instructions">压缩指令</label><input class="compact-input" id="compact-instructions" placeholder="保留结论与引用"><button class="action" id="compact-chat">压缩上下文</button><label for="fork-entry">分支起点</label><select id="fork-entry"><option value="">先加载会话节点</option></select><button class="action" id="fork-chat">创建分支</button><button class="action" id="load-entries">刷新会话节点</button></div></aside></div></section>
    <section data-page="evaluations" hidden><div class="section-head"><div><h2>评测实验室</h2><p>确定性检查 · 证据快照 · 回归基线</p></div><div class="toolbar"><button class="action primary" id="new-eval-case">新建 Case</button><button class="action" id="refresh-evaluations">刷新</button></div></div><div class="state" data-state="evaluations">等待加载</div><div class="eval-layout"><div class="stack"><article class="card"><div class="subhead">评测 Case</div><div class="stack" id="eval-case-list"></div></article><article class="card"><div class="row"><h3>Case 编辑器</h3><span class="editor-mode" id="eval-editor-mode">新建 CASE</span></div><form class="form-grid" id="eval-case-form"><label>名称<input id="eval-name" required maxlength="200"></label><label>通过阈值<input id="eval-threshold" type="number" min="0" max="1" step="0.01" value="1" required></label><label class="wide">Prompt<textarea id="eval-prompt" required maxlength="20000"></textarea></label><label class="wide">必须调用的工具<input id="eval-required-tools" placeholder="search_papers, validate_citation"></label><label class="wide">禁止调用的工具<input id="eval-forbidden-tools" placeholder="unsafe_tool"></label><label>最大工具调用<input id="eval-max-tools" type="number" min="0" max="1000"></label><label>最大耗时 ms<input id="eval-max-duration" type="number" min="1" max="86400000"></label><label class="wide">回答必须包含<input id="eval-answer-contains" placeholder="evidence, citation"></label><label>终态<select id="eval-terminal-status"><option value="">不限制</option><option value="completed">completed</option><option value="failed">failed</option><option value="aborted">aborted</option></select></label><label class="check-label"><input id="eval-require-citation" type="checkbox">必须成功执行 validate_citation</label><label class="wide">必须使用的 Memory ID<input id="eval-required-memories" placeholder="memory-id-1, memory-id-2"></label><label class="wide">禁止使用的 Memory ID<input id="eval-forbidden-memories" placeholder="memory-id-secret"></label><label>上下文状态<select id="eval-context-status"><option value="">不限制</option><option value="selected">selected</option><option value="empty">empty</option><option value="error">error</option></select></label><label>最大上下文条数<input id="eval-max-context-items" type="number" min="0" max="50"></label><button class="action primary wide" id="save-eval-case" type="submit">保存 Case</button></form></article></div><div class="stack"><article class="card"><div class="row"><h3>执行或评测</h3><span class="pill" id="eval-selection">未选择 Case</span></div><p class="muted">直接执行 Case Prompt，或对已结束的 Run 重放确定性检查。</p><div class="toolbar"><button class="action primary" id="execute-eval-case">执行 Case Prompt</button><label for="eval-run-select">已结束的 Run</label><select id="eval-run-select"><option value="">没有已结束的 Run</option></select><button class="action" id="run-evaluation">评测已有 Run</button></div></article><article class="card"><div class="subhead">得分历史</div><div class="state" data-state="eval-history">选择 Case</div><div class="stack" id="eval-history"></div><div class="subhead">已保存的结果</div><div class="stack" id="eval-result-list"></div></article><article class="card" id="eval-result-detail"><div class="state">选择一条评测结果</div></article></div></div></section>
    <section data-page="evaluation-suites" hidden><div class="section-head"><div><h2>评测 Suite</h2><p>有序 Case · 隔离执行 · 不可变汇总历史</p></div><div class="toolbar"><button class="action" id="show-eval-cases">Case 列表</button><button class="action primary" id="new-eval-suite">新建 Suite</button><button class="action" id="refresh-eval-suites">刷新</button></div></div><div class="state" data-state="evaluation-suites">等待加载</div><div class="eval-layout"><div class="stack"><article class="card"><div class="subhead">Suite 定义</div><div class="stack" id="eval-suite-list"></div></article><article class="card"><div class="row"><h3>Suite 编辑器</h3><span class="editor-mode" id="eval-suite-editor-mode">新建 SUITE</span></div><form class="form-grid" id="eval-suite-form"><label class="wide">名称<input id="eval-suite-name" required maxlength="200"></label><label class="wide">添加 Case<select id="eval-suite-case-select"><option value="">没有可添加的 Case</option></select></label><button class="action wide" id="add-eval-suite-case" type="button">添加到执行序列</button><div class="wide"><div class="subhead">Case 执行顺序</div><div class="stack" id="eval-suite-members"></div></div><button class="action primary wide" id="save-eval-suite" type="submit">保存 Suite</button></form></article></div><div class="stack"><article class="card"><div class="row"><h3>执行 Suite</h3><span class="pill" id="eval-suite-selection">未选择 Suite</span></div><p class="muted">每个 Case 使用独立 MiniPy 会话；后端提供最终汇总结果。</p><button class="action primary" id="execute-eval-suite">执行整套回归</button></article><article class="card"><div class="subhead">Suite 运行历史</div><div class="state" data-state="eval-suite-history">选择 Suite</div><div class="stack" id="eval-suite-run-list"></div></article><article class="card" id="eval-suite-run-detail"><div class="state">选择一条 Suite 运行</div></article></div></div></section>
  </main></div>
  <script type="module">
    const state={runs:[],memories:[],papers:[],selectedRun:null,evaluationCases:[],evaluationResults:[],evaluationRuns:[],selectedCase:null,selectedResult:null,evaluationSuites:[],evaluationSuiteRuns:[],selectedSuite:null,selectedSuiteRun:null,suiteDraftName:"",suiteDraftCaseIds:[],chat:{session:null,socket:null,busy:false,connectionError:false}};const titles={overview:"系统总览",guide:"使用指南",chat:"Agent 对话",runs:"运行追踪",evaluations:"评测实验室","evaluation-suites":"评测 Suite",memories:"记忆审核",library:"文献库"};const statusLabels={completed:"已完成",running:"运行中",failed:"失败",aborted:"已中止",candidate:"候选",confirmed:"已确认",rejected:"已拒绝",superseded:"已替代",passed:"通过",regression:"回归",connected:"已连接",disconnected:"未连接",idle:"空闲",selected:"已选择",empty:"空",error:"错误"};const byId=id=>document.getElementById(id);const make=(tag,className,text)=>{const item=document.createElement(tag);if(className)item.className=className;if(text!==undefined)item.textContent=String(text);return item};const clear=target=>target.replaceChildren();const pretty=value=>JSON.stringify(value,null,2);const time=value=>value?new Date(value).toLocaleString():"—";const badge=value=>make("span",`pill ${value||""}`,statusLabels[value]||value||"未知");
    function setPanelState(panel,kind,message,visible=true){let target=document.querySelector(`[data-state="${panel}"]`);if(!target){const host=byId(panel);if(!host)return;target=make("div","state");target.dataset.state=panel;host.prepend(target)}target.dataset.kind=kind;target.textContent=message;target.hidden=!visible}
    async function request(path,options){const response=await fetch(path,options);if(!response.ok){let message=`${response.status} ${response.statusText}`;try{const body=await response.json();if(typeof body.detail==="string")message=body.detail;else if(body.detail)message=JSON.stringify(body.detail)}catch(_){void _}throw new Error(message)}return response.status===204?null:response.json()}
    function runRow(run,clickable=false){const item=make(clickable?"button":"div",clickable?"list-button":"memory-card");if(clickable)item.type="button";const top=make("div","row");top.append(make("strong","mono",run.runtime_type),badge(run.status));item.append(top,make("small","mono",run.id),make("small","",`${time(run.started_at)} · 活跃节点 ${run.active_leaf_id||"—"}`));if(clickable)item.addEventListener("click",()=>selectRun(run.id));return item}
    async function loadOverview(){setPanelState("overview","loading","正在汇总运行、工具、记忆、文献与评测数据…");try{const[runs,memories,papers,evaluations]=await Promise.all([request("/runs?limit=100"),request("/memories?limit=500"),request("/papers?limit=500"),request("/evaluations/results?limit=500")]);const toolGroups=await Promise.all(runs.map(run=>request(`/runs/${encodeURIComponent(run.id)}/tools`).catch(()=>[])));state.runs=runs;state.memories=memories;state.papers=papers;byId("metric-runs").textContent=runs.length;byId("metric-tools").textContent=toolGroups.reduce((total,group)=>total+group.length,0);byId("metric-candidates").textContent=memories.filter(item=>item.status==="candidate").length;byId("metric-confirmed").textContent=memories.filter(item=>item.status==="confirmed").length;byId("metric-papers").textContent=papers.length;byId("metric-evals").textContent=evaluations.length;byId("metric-eval-passed").textContent=evaluations.filter(item=>item.passed).length;byId("metric-regressions").textContent=evaluations.filter(item=>item.regression).length;const recent=byId("recent-runs");clear(recent);runs.slice(0,6).forEach(run=>recent.append(runRow(run,true)));setPanelState("overview",runs.length?"ready":"empty",runs.length?"":"还没有 Agent 运行。请先在“Agent 对话”发送任务，或运行 Pi。",!runs.length)}catch(error){setPanelState("overview","error",`总览加载失败：${error.message}`)}}
    async function loadRuns(){setPanelState("runs","loading","正在读取运行索引…");const list=byId("runs-list");clear(list);try{state.runs=await request("/runs?limit=100");state.runs.forEach(run=>list.append(runRow(run,true)));setPanelState("runs",state.runs.length?"ready":"empty",state.runs.length?"":"没有可检查的运行。",!state.runs.length);if(state.runs.length)await selectRun(state.selectedRun||state.runs[0].id)}catch(error){setPanelState("runs","error",`运行列表失败：${error.message}`)}}
    async function selectRun(runId){state.selectedRun=runId;const detail=byId("run-detail");setPanelState("run-detail","loading","正在读取事件和工具调用…");try{const[run,events,tools]=await Promise.all([request(`/runs/${encodeURIComponent(runId)}`),request(`/runs/${encodeURIComponent(runId)}/events`),request(`/runs/${encodeURIComponent(runId)}/tools`)]);clear(detail);const head=make("div","row");head.append(make("h2","",run.runtime_type),badge(run.status));detail.append(head);const fields=make("dl","detail-grid");[["Run ID",run.id],["开始时间",time(run.started_at)],["结束时间",time(run.ended_at)],["活跃节点",run.active_leaf_id||"—"],["会话",run.external_session_id||"—"],["错误",run.error||"—"]].forEach(([key,value])=>{const box=make("div");box.append(make("dt","",key),make("dd","",value));fields.append(box)});detail.append(fields,make("div","subhead",`4e8b件时间线 · ${events.length}`));const timeline=make("div","timeline");events.forEach(event=>{const item=make("article","timeline-item"),row=make("div","row");row.append(make("h3","mono",`${event.sequence}. ${event.event_type}`),make("span","muted mono",time(event.event_timestamp)));item.append(row);if(event.entry_id)item.append(make("div","muted mono",`entry ${event.entry_id}`));item.append(make("pre","",pretty(event.payload)));timeline.append(item)});detail.append(timeline,make("div","subhead",`5de5具调用检查器 · ${tools.length}`));const toolList=make("div","stack");tools.forEach(tool=>{const item=make("article","tool-card"),row=make("div","row");row.append(make("h3","mono",tool.tool_name),badge(tool.is_error?"failed":"completed"));item.append(row,make("div","muted mono",`${tool.tool_call_id} · ${tool.duration_ms??"—"} ms`),make("pre","",`入参\n${pretty(tool.arguments)}\n\n返回值\n${pretty(tool.result)}`));toolList.append(item)});if(!events.length)detail.append(make("div","state","该运行没有事件。"));if(!tools.length)toolList.append(make("div","state","该运行没有工具调用。"));detail.append(toolList)}catch(error){clear(detail);const panel=make("div","state",`运行详情失败：${error.message}`);panel.dataset.kind="error";detail.append(panel)}}
    async function loadMemories(){setPanelState("memories","loading","正在读取 Memory 信任库…");const list=byId("memory-list");clear(list);const status=byId("memory-filter").value,suffix=status?`?status=${encodeURIComponent(status)}&limit=500`:"?limit=500";try{const memories=await request(`/memories${suffix}`);state.memories=memories;memories.forEach(memory=>list.append(memoryCard(memory)));setPanelState("memories",memories.length?"ready":"empty",memories.length?"":"当前筛选没有记忆。",!memories.length)}catch(error){setPanelState("memories","error",`记忆加载失败：${error.message}`)}}
    function memoryCard(memory){const item=make("article","memory-card"),top=make("div","row");top.append(badge(memory.status),make("span","mono muted",`${memory.kind} · ${memory.scope} · ${Math.round(memory.confidence*100)}%`));item.append(top,make("p","",memory.content));memory.evidence.forEach(e=>item.append(make("blockquote","",`${e.paper_id} / ${e.passage_id}${e.page?` · p.${e.page}`:""}\n“${e.quote}”`)));const provenance=[memory.trace_run_id&&`run ${memory.trace_run_id}`,memory.source_tool_call_id&&`tool ${memory.source_tool_call_id}`].filter(Boolean).join(" · ");if(provenance)item.append(make("div","muted mono",provenance));if(memory.status==="candidate"){const actions=make("div","toolbar"),confirm=make("button","action primary","确认"),reject=make("button","action danger","拒绝");confirm.type=reject.type="button";confirm.addEventListener("click",()=>reviewMemory(memory.id,"confirm",[confirm,reject]));reject.addEventListener("click",()=>reviewMemory(memory.id,"reject",[confirm,reject]));actions.append(confirm,reject);item.append(actions)}return item}
    async function reviewMemory(memoryId,action,buttons){buttons.forEach(button=>{button.disabled=true});try{await request(`/memories/${encodeURIComponent(memoryId)}/${action}`,{method:"POST"});await Promise.all([loadMemories(),loadOverview()])}catch(error){setPanelState("memories","error",`审核失败：${error.message}`);buttons.forEach(button=>{button.disabled=false})}}
    async function loadLibrary(){setPanelState("library","loading","正在读取文献目录…");const list=byId("paper-list");clear(list);try{const papers=await request("/papers?limit=500");state.papers=papers;papers.forEach(paper=>{const item=make("article","paper-card"),top=make("div","row");top.append(make("h3","",paper.title),make("span","pill",`${paper.passage_count} 个片段`));item.append(top,make("div","muted",`${paper.authors.join(", ")||"未知作者"}${paper.year?` · ${paper.year}`:""}`),make("div","mono muted",paper.id));list.append(item)});setPanelState("library",papers.length?"ready":"empty",papers.length?"":"文献库为空，可通过 API 导入 PDF。",!papers.length)}catch(error){setPanelState("library","error",`目录加载失败：${error.message}`)}}
    async function searchLibrary(event){event.preventDefault();const query=byId("search-query").value.trim();if(!query)return;setPanelState("search","loading","正在执行检索…");const results=byId("search-results");clear(results);try{const body=await request("/internal/tools/search_papers",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({query,mode:byId("search-mode").value,limit:20})});body.items.forEach(hit=>{const item=make("article","result-card"),top=make("div","row");top.append(make("h3","",hit.title),make("span","pill",hit.retrieval_mode));item.append(top,make("div","mono muted",`${hit.paper_id} / ${hit.passage_id}${hit.page?` · p.${hit.page}`:""}`),make("p","",hit.text),make("div","mono muted",`综合得分 ${Number(hit.score).toFixed(5)} · 关键词排名 ${hit.lexical_rank??"—"} · 向量排名 ${hit.vector_rank??"—"}`));results.append(item)});setPanelState("search",body.items.length?"ready":"empty",body.items.length?"":"没有找到相关文献片段。",!body.items.length)}catch(error){setPanelState("search","error",`检索失败：${error.message}`)}}
    function chatStatus(kind,message){setPanelState("chat",kind,message,true);byId("chat-connection").textContent=state.chat.socket?.readyState===WebSocket.OPEN?"已连接":"未连接";byId("chat-busy").textContent=state.chat.busy?"运行中":"空闲";byId("send-chat").disabled=state.chat.busy||state.chat.socket?.readyState!==WebSocket.OPEN||Boolean(state.graph?.nodes.length&&state.graph.nodes.find(node=>node.id===state.selectedTurnId)?.continue_entry_id!==state.graph.active_leaf_id);byId("abort-chat").disabled=!state.chat.busy;byId("compact-chat").disabled=state.chat.busy||state.chat.socket?.readyState!==WebSocket.OPEN;byId("fork-chat").disabled=state.chat.busy||state.chat.socket?.readyState!==WebSocket.OPEN;byId("delete-chat").disabled=!state.chat.session}
    function appendChat(role,text){const item=make("div",`chat-message ${role}`,text);byId("chat-log").append(item);item.scrollIntoView({block:"nearest"})}
    function appendActivity(label,payload){const item=make("div","activity-item"),title=make("strong","mono",label);item.append(title);if(payload!==undefined)item.append(make("pre","",pretty(payload)));byId("chat-activity").prepend(item)}
    function renderChatEntries(entries){const select=byId("fork-entry");clear(select);if(!entries.length){const option=make("option","","没有会话节点");option.value="";select.append(option);return}entries.forEach(entry=>{const role=entry.data.role||entry.type,content=String(entry.data.content||"").slice(0,42),option=make("option","",`${role} · ${content||entry.entry_id}`);option.value=entry.entry_id;select.append(option)})}
    function sendChatCommand(command){if(state.chat.socket?.readyState===WebSocket.OPEN)state.chat.socket.send(JSON.stringify(command));else chatStatus("error","会话未连接，请新建或重连。")}
    function handleChatMessage(message){if(message.type==="session_ready"){state.chat.session=message.session;byId("chat-session-id").textContent=`session ${message.session.id}`;state.chat.busy=message.session.busy;chatStatus("ready","会话已连接，可以发送研究任务。");sendChatCommand({type:"entries"});return}if(message.type==="event"){const event=message.event;if(event.type==="agent_start"){state.chat.busy=true;chatStatus("loading","Agent 正在运行…")}else if(event.type==="message_update")appendChat("assistant",event.data.delta||"");else if(event.type==="context_injection")appendActivity(`Memory 上下文 · ${event.data.status}`,event.data);else if(event.type==="tool_execution_start")appendActivity(`工具开始 · ${event.data.toolName}`,event.data.args);else if(event.type==="tool_execution_end")appendActivity(`工具${event.data.isError?"失败":"完成"} · ${event.data.toolName}`,event.data.result);else appendActivity(event.type,event.data);return}if(message.type==="turn_complete"){state.chat.session=message.session;state.chat.busy=false;if(message.error)chatStatus("error",`本轮失败：${message.error} · trace ${message.session.last_run_id||"—"}`);else chatStatus("ready",`本轮完成 · trace ${message.session.last_run_id||"—"}`);sendChatCommand({type:"entries"});return}if(message.type==="command_result"){if(message.session)state.chat.session=message.session;if(message.command==="entries")renderChatEntries(message.entries||[]);else chatStatus("ready",`${message.command} 已完成。`);return}if(message.type==="error"){state.chat.connectionError=true;if(message.code==="turn_failed")state.chat.busy=false;chatStatus("error",`${message.code}：${message.message}`)}}
    function connectChat(){if(!state.chat.session){chatStatus("empty","没有可重连的会话，请新建会话。");return}if(state.chat.socket)state.chat.socket.close();const scheme=location.protocol==="https:"?"wss:":"ws:",socket=new WebSocket(`${scheme}//${location.host}/chat/sessions/${encodeURIComponent(state.chat.session.id)}/stream`);state.chat.socket=socket;state.chat.connectionError=false;chatStatus("loading","正在连接 Runtime…");socket.addEventListener("message",event=>{try{handleChatMessage(JSON.parse(event.data))}catch(error){chatStatus("error",`消息解析失败：${error.message}`)}});socket.addEventListener("close",event=>{if(state.chat.socket===socket){state.chat.socket=null;state.chat.busy=false;if(!state.chat.connectionError){if(event.code===4403)chatStatus("error","服务端模型配置不可用；持久会话仍已保留。");else if(event.code===4410)chatStatus("error","持久会话校验失败，已拒绝恢复。");else chatStatus("empty","会话已断开，可点击重连。")}}});socket.addEventListener("error",()=>chatStatus("error","WebSocket 连接失败。"))}
    async function newChat(){chatStatus("loading","正在创建独立 MiniPy 会话…");try{if(state.chat.socket)state.chat.socket.close();state.chat.session=await request("/chat/sessions",{method:"POST"});clear(byId("chat-log"));clear(byId("chat-activity"));connectChat()}catch(error){chatStatus("error",`创建失败：${error.message}`)}}
    async function loadChat(){if(state.chat.socket?.readyState===WebSocket.OPEN){chatStatus(state.chat.busy?"loading":"ready",state.chat.busy?"Agent 正在运行…":"会话已连接，可以发送研究任务。");return}try{const sessions=await request("/chat/sessions");if(state.chat.session){const current=sessions.find(item=>item.id===state.chat.session.id);state.chat.session=current||null}else state.chat.session=sessions[0]||null;if(state.chat.session){byId("chat-session-id").textContent=`session ${state.chat.session.id}`;connectChat()}else chatStatus("empty","尚无会话。设置服务端 OPENAI_MODEL 后点击“新建会话”。")}catch(error){chatStatus("error",`会话列表失败：${error.message}`)}}
    async function deleteChat(){if(!state.chat.session)return;const id=state.chat.session.id;if(state.chat.socket)state.chat.socket.close();try{await request(`/chat/sessions/${encodeURIComponent(id)}`,{method:"DELETE"});state.chat.session=null;state.chat.socket=null;state.chat.busy=false;clear(byId("chat-log"));clear(byId("chat-activity"));byId("chat-session-id").textContent="session —";chatStatus("empty","会话已删除。") }catch(error){chatStatus("error",`删除失败：${error.message}`)}}
    function submitChat(event){event.preventDefault();const content=byId("chat-prompt").value.trim();if(!content||state.chat.busy)return;if(state.graph?.nodes.length&&state.graph.nodes.find(node=>node.id===state.selectedTurnId)?.continue_entry_id!==state.graph.active_leaf_id){chatStatus("error","请先点击“从此继续”或“重试此轮”，再发送任务。");return}appendChat("user",content);byId("chat-prompt").value="";state.chat.busy=true;chatStatus("loading","任务已发送，等待 Runtime 事件…");sendChatCommand({type:"prompt",content})}
    function parseList(value){return[...new Set(value.split(",").map(item=>item.trim()).filter(Boolean))]}
    function evaluationCaseRow(item){const button=make("button",`list-button${state.selectedCase?.id===item.id?" selected":""}`);button.type="button";const top=make("div","row");top.append(make("strong","",item.name),make("span","pill",`${Math.round(item.pass_threshold*100)}%`));button.append(top,make("small","",item.prompt),make("small","mono",item.id));button.addEventListener("click",()=>selectEvaluationCase(item.id));return button}
    function fillEvaluationForm(item){byId("eval-name").value=item?.name||"";byId("eval-prompt").value=item?.prompt||"";byId("eval-threshold").value=item?.pass_threshold??1;const expected=item?.expectations||{};byId("eval-required-tools").value=(expected.required_tools||[]).join(", ");byId("eval-forbidden-tools").value=(expected.forbidden_tools||[]).join(", ");byId("eval-max-tools").value=expected.max_tool_calls??"";byId("eval-max-duration").value=expected.max_duration_ms??"";byId("eval-answer-contains").value=(expected.answer_contains||[]).join(", ");byId("eval-terminal-status").value=expected.terminal_status||"";byId("eval-require-citation").checked=Boolean(expected.require_citation_validation);byId("eval-required-memories").value=(expected.required_memory_ids||[]).join(", ");byId("eval-forbidden-memories").value=(expected.forbidden_memory_ids||[]).join(", ");byId("eval-context-status").value=expected.context_status||"";byId("eval-max-context-items").value=expected.max_context_items??"";byId("eval-editor-mode").textContent=item?"编辑 CASE":"新建 CASE"}
    function newEvaluationCase(){state.selectedCase=null;state.selectedResult=null;fillEvaluationForm(null);renderEvaluationCases();renderEvaluationResults();byId("eval-selection").textContent="未选择 Case";setPanelState("evaluations","ready","正在创建新 Case；至少填写一条预期规则。")}
    function renderEvaluationCases(){const list=byId("eval-case-list");clear(list);state.evaluationCases.forEach(item=>list.append(evaluationCaseRow(item)));if(!state.evaluationCases.length)list.append(make("div","state","还还没有评测 Case。"))}
    function populateEvaluationRuns(){const select=byId("eval-run-select"),terminal=state.evaluationRuns.filter(run=>run.status!=="running");clear(select);if(!terminal.length){const option=make("option","","没有已结束的 Run");option.value="";select.append(option);return}terminal.forEach(run=>{const option=make("option","",`${run.runtime_type} · ${run.status} · ${time(run.started_at)}`);option.value=run.id;select.append(option)})}
    function renderEvaluationResults(){const list=byId("eval-result-list"),history=byId("eval-history"),detail=byId("eval-result-detail");clear(list);clear(history);const results=state.selectedCase?state.evaluationResults.filter(item=>item.case_id===state.selectedCase.id):[];results.forEach(result=>{const button=make("button",`list-button${state.selectedResult?.id===result.id?" selected":""}`);button.type="button";button.dataset.resultId=result.id;const top=make("div","row");top.append(make("strong","mono",`${Math.round(result.score*100)}%`),badge(result.regression?"regression":result.passed?"passed":"failed"));button.append(top,make("small","",`${result.runtime_type} · ${time(result.evaluated_at)}`),make("small","mono",result.run_id));button.addEventListener("click",()=>selectEvaluationResult(result.id));list.append(button)});results.slice().reverse().forEach(result=>{const row=make("div",`history-row${result.regression?" regression":""}`),label=make("span","mono",time(result.evaluated_at)),track=make("div","score-track"),bar=make("div","score-bar"),delta=result.score_delta===null?"基线":`${result.score_delta>=0?"+":""}${Math.round(result.score_delta*100)} pts${result.regression?" · 回归":""}`;bar.style.width=`${Math.max(0,Math.min(100,result.score*100))}%`;track.append(bar);row.append(label,track,make("span","mono",`${Math.round(result.score*100)}% · ${delta}`));history.append(row)});setPanelState("eval-history",results.length?"ready":"empty",results.length?"":"该 Case 尚无评测结果。",!results.length);if(!results.length){clear(detail);detail.append(make("div","state","执行一次评测后查看 检查证据。"));state.selectedResult=null}else if(!state.selectedResult||!results.some(item=>item.id===state.selectedResult.id))selectEvaluationResult(results[0].id)}
    function selectEvaluationCase(caseId){const item=state.evaluationCases.find(candidate=>candidate.id===caseId);if(!item)return;state.selectedCase=item;state.selectedResult=null;byId("eval-selection").textContent=item.name;fillEvaluationForm(item);renderEvaluationCases();renderEvaluationResults();setPanelState("evaluations","ready",`已选择 Case：${item.name}`)}
    function selectEvaluationResult(resultId){const result=state.evaluationResults.find(item=>item.id===resultId);if(!result)return;state.selectedResult=result;const detail=byId("eval-result-detail");clear(detail);const top=make("div","row");top.append(make("h2","",result.case_name),badge(result.regression?"regression":result.passed?"passed":"failed"));detail.append(top);const fields=make("dl","detail-grid");[["得分",`${Math.round(result.score*100)}%`],["通过阈值",`${Math.round(result.pass_threshold*100)}%`],["Runtime",result.runtime_type],["Run 状态",result.run_status],["上次得分",result.previous_score===null?"—":`${Math.round(result.previous_score*100)}%`],["变化",result.score_delta===null?"—":`${result.score_delta>=0?"+":""}${Math.round(result.score_delta*100)} pts`],["评测时间",time(result.evaluated_at)],["Run ID",result.run_id]].forEach(([key,value])=>{const box=make("div");box.append(make("dt","",key),make("dd","",value));fields.append(box)});detail.append(fields,make("div","subhead",`检查项 · ${result.checks.length}`));const checks=make("div","stack");result.checks.forEach(check=>{const card=make("article","check-card"),row=make("div","row"),evidence=make("div","check-evidence"),expected=make("div"),observed=make("div");row.append(make("h3","mono",check.id),badge(check.passed?"passed":"failed"));expected.append(make("span","muted","预期值"),make("pre","",pretty(check.expected)));observed.append(make("span","muted","实际值"),make("pre","",pretty(check.observed)));evidence.append(expected,observed);card.append(row,make("p","",check.message),evidence);checks.append(card)});detail.append(checks);renderEvaluationCases();byId("eval-result-list").querySelectorAll(".list-button").forEach(button=>button.classList.toggle("selected",button.dataset.resultId===result.id))}
    async function loadEvaluations(){setPanelState("evaluations","loading","正在读取 Case、已结束的 Run 与结果历史…");try{const[cases,results,runs]=await Promise.all([request("/evaluations/cases?limit=500"),request("/evaluations/results?limit=500"),request("/runs?limit=500")]);state.evaluationCases=cases;state.evaluationResults=results;state.evaluationRuns=runs;populateEvaluationRuns();const selected=state.selectedCase&&cases.find(item=>item.id===state.selectedCase.id);state.selectedCase=selected||cases[0]||null;renderEvaluationCases();if(state.selectedCase)selectEvaluationCase(state.selectedCase.id);else{newEvaluationCase();setPanelState("evaluations","empty","还还没有评测 Case，请创建第一条。")}}catch(error){setPanelState("evaluations","error",`评测实验室加载失败：${error.message}`)}}
    async function saveEvaluationCase(event){event.preventDefault();const button=byId("save-eval-case"),expectations={required_tools:parseList(byId("eval-required-tools").value),forbidden_tools:parseList(byId("eval-forbidden-tools").value),answer_contains:parseList(byId("eval-answer-contains").value),require_citation_validation:byId("eval-require-citation").checked,required_memory_ids:parseList(byId("eval-required-memories").value),forbidden_memory_ids:parseList(byId("eval-forbidden-memories").value)};if(byId("eval-max-tools").value!=="")expectations.max_tool_calls=Number(byId("eval-max-tools").value);if(byId("eval-max-duration").value!=="")expectations.max_duration_ms=Number(byId("eval-max-duration").value);if(byId("eval-terminal-status").value)expectations.terminal_status=byId("eval-terminal-status").value;if(byId("eval-context-status").value)expectations.context_status=byId("eval-context-status").value;if(byId("eval-max-context-items").value!=="")expectations.max_context_items=Number(byId("eval-max-context-items").value);const payload={name:byId("eval-name").value.trim(),prompt:byId("eval-prompt").value.trim(),pass_threshold:Number(byId("eval-threshold").value),expectations},path=state.selectedCase?`/evaluations/cases/${encodeURIComponent(state.selectedCase.id)}`:"/evaluations/cases",method=state.selectedCase?"PUT":"POST";button.disabled=true;setPanelState("evaluations","loading",state.selectedCase?"正在更新 Case…":"正在创建 Case…");try{const saved=await request(path,{method,headers:{"content-type":"application/json"},body:JSON.stringify(payload)});state.selectedCase=saved;await loadEvaluations()}catch(error){setPanelState("evaluations","error",`保存失败：${error.message}`)}finally{button.disabled=false}}
    async function runEvaluation(){const runId=byId("eval-run-select").value,button=byId("run-evaluation");if(!state.selectedCase){setPanelState("evaluations","error","请先选择或创建 Case。");return}if(!runId){setPanelState("evaluations","error","没有可评的已结束 Run。");return}button.disabled=true;setPanelState("evaluations","loading","正在执行确定性 Trace 检查…");try{const result=await request(`/evaluations/cases/${encodeURIComponent(state.selectedCase.id)}/runs/${encodeURIComponent(runId)}`,{method:"POST"});state.selectedResult=result;await Promise.all([loadEvaluations(),loadOverview()]);selectEvaluationResult(result.id)}catch(error){setPanelState("evaluations","error",`评测失败：${error.message}`)}finally{button.disabled=false}}
    async function executeEvaluationCase(){const button=byId("execute-eval-case");if(!state.selectedCase){setPanelState("evaluations","error","请先选择或创建 Case。");return}button.disabled=true;setPanelState("evaluations","loading","正在独立 MiniPy 会话中执行 Case Prompt…");try{const execution=await request(`/evaluations/cases/${encodeURIComponent(state.selectedCase.id)}/execute`,{method:"POST"});state.selectedResult=execution.result;await Promise.all([loadEvaluations(),loadOverview()]);selectEvaluationResult(execution.result.id);const outcome=execution.runtime_error?` · ${execution.runtime_error}`:"";setPanelState("evaluations",execution.runtime_error?"error":"ready",`执行完成 · run ${execution.run_id} · ${execution.event_count} events${outcome}`)}catch(error){setPanelState("evaluations","error",`Case 执行失败：${error.message}`)}finally{button.disabled=false}}
    function suiteCase(caseId){return state.evaluationCases.find(item=>item.id===caseId)}
    function renderSuiteDefinitions(){const list=byId("eval-suite-list");clear(list);state.evaluationSuites.forEach(suite=>{const button=make("button",`list-button${state.selectedSuite?.id===suite.id?" selected":""}`);button.type="button";const top=make("div","row");top.append(make("strong","",suite.name),make("span","pill",`${suite.case_ids.length} 个 Case`));button.append(top,make("small","mono",suite.id));button.addEventListener("click",()=>selectEvaluationSuite(suite.id));list.append(button)});if(!state.evaluationSuites.length)list.append(make("div","state","还没有评测 Suite。"))}
    function renderSuiteEditor(){byId("eval-suite-name").value=state.suiteDraftName;byId("eval-suite-editor-mode").textContent=state.selectedSuite?"编辑 SUITE":"新建 SUITE";const select=byId("eval-suite-case-select");clear(select);const available=state.evaluationCases.filter(item=>!state.suiteDraftCaseIds.includes(item.id));if(!available.length){const option=make("option","","没有可添加的 Case");option.value="";select.append(option)}else available.forEach(item=>{const option=make("option","",item.name);option.value=item.id;select.append(option)});const members=byId("eval-suite-members");clear(members);state.suiteDraftCaseIds.forEach((caseId,index)=>{const item=suiteCase(caseId),row=make("article","check-card"),top=make("div","row"),controls=make("div","toolbar");top.append(make("strong","",`${index+1}. ${item?.name||caseId}`));[["↑","上移",-1],["↓","下移",1],["移除","移除",0]].forEach(([label,title,offset])=>{const button=make("button","action",label);button.type="button";button.title=title;button.disabled=offset<0?index===0:offset>0?index===state.suiteDraftCaseIds.length-1:false;button.addEventListener("click",()=>offset===0?removeSuiteCase(index):moveSuiteCase(index,offset));controls.append(button)});top.append(controls);row.append(top,make("small","mono",caseId));members.append(row)});if(!state.suiteDraftCaseIds.length)members.append(make("div","state","至少添加一个 Case。"))}
    function moveSuiteCase(index,offset){const target=index+offset;if(target<0||target>=state.suiteDraftCaseIds.length)return;const draft=[...state.suiteDraftCaseIds];[draft[index],draft[target]]=[draft[target],draft[index]];state.suiteDraftCaseIds=draft;renderSuiteEditor()}
    function removeSuiteCase(index){state.suiteDraftCaseIds=state.suiteDraftCaseIds.filter((_id,position)=>position!==index);renderSuiteEditor()}
    function addSuiteCase(){const caseId=byId("eval-suite-case-select").value;if(!caseId||state.suiteDraftCaseIds.includes(caseId))return;state.suiteDraftCaseIds=[...state.suiteDraftCaseIds,caseId];renderSuiteEditor()}
    function newEvaluationSuite(){state.selectedSuite=null;state.selectedSuiteRun=null;state.suiteDraftName="";state.suiteDraftCaseIds=[];renderSuiteDefinitions();renderSuiteEditor();renderSuiteRuns();byId("eval-suite-selection").textContent="未选择 Suite";setPanelState("evaluation-suites","ready","正在创建新 Suite；请按执行顺序添加 Case。")}
    function selectEvaluationSuite(suiteId){const suite=state.evaluationSuites.find(item=>item.id===suiteId);if(!suite)return;state.selectedSuite=suite;state.selectedSuiteRun=null;state.suiteDraftName=suite.name;state.suiteDraftCaseIds=[...suite.case_ids];byId("eval-suite-selection").textContent=suite.name;renderSuiteDefinitions();renderSuiteEditor();renderSuiteRuns();setPanelState("evaluation-suites","ready",`已选择 Suite：${suite.name}`)}
    function renderSuiteRuns(){const list=byId("eval-suite-run-list"),detail=byId("eval-suite-run-detail");clear(list);const runs=state.selectedSuite?state.evaluationSuiteRuns.filter(run=>run.suite_id===state.selectedSuite.id):[];runs.forEach(run=>{const button=make("button",`list-button${state.selectedSuiteRun?.id===run.id?" selected":""}`);button.type="button";button.dataset.suiteRunId=run.id;const top=make("div","row");top.append(make("strong","",`${run.passed_count}/${run.total_count} 通过`),badge(run.passed?"passed":"failed"));button.append(top,make("small","",`${run.failed_count} 失败 · ${run.error_count} 错误 · ${time(run.started_at)}`),make("small","mono",run.id));button.addEventListener("click",()=>selectEvaluationSuiteRun(run.id));list.append(button)});setPanelState("eval-suite-history",runs.length?"ready":"empty",runs.length?"":"该 Suite 尚无执行历史。",!runs.length);if(!runs.length){clear(detail);detail.append(make("div","state","执行 Suite 后查看 汇总和逐 Case 结果。"));state.selectedSuiteRun=null}else if(!state.selectedSuiteRun||!runs.some(run=>run.id===state.selectedSuiteRun.id))selectEvaluationSuiteRun(runs[0].id)}
    function selectEvaluationSuiteRun(runId){const run=state.evaluationSuiteRuns.find(item=>item.id===runId);if(!run)return;state.selectedSuiteRun=run;const detail=byId("eval-suite-run-detail");clear(detail);const top=make("div","row");top.append(make("h2","",run.suite_name),badge(run.passed?"passed":"failed"));detail.append(top);const fields=make("dl","detail-grid");[["总数",run.total_count],["通过",run.passed_count],["失败",run.failed_count],["错误",run.error_count],["开始时间",time(run.started_at)],["结束时间",time(run.ended_at)],["Suite ID",run.suite_id],["Suite Run ID",run.id]].forEach(([key,value])=>{const box=make("div");box.append(make("dt","",key),make("dd","",value));fields.append(box)});detail.append(fields,make("div","subhead",`有序执行项 · ${run.items.length}`));const items=make("div","stack");run.items.forEach(item=>{const card=make("article","check-card"),row=make("div","row"),status=item.error?"failed":item.passed?"passed":"failed";row.append(make("h3","",`${item.position+1}. ${item.case_name}`),badge(status));card.append(row,make("p","muted",item.case_prompt));const metadata=make("dl","detail-grid");[["得分",item.score===null?"—":`${Math.round(item.score*100)}%`],["events",item.event_count],["runtime error",item.runtime_error||"—"],["execution error",item.error||"—"],["Run ID",item.run_id||"—"],["result id",item.result_id||"—"]].forEach(([key,value])=>{const box=make("div");box.append(make("dt","",key),make("dd","",value));metadata.append(box)});card.append(metadata);items.append(card)});detail.append(items);byId("eval-suite-run-list").querySelectorAll(".list-button").forEach(button=>button.classList.toggle("selected",button.dataset.suiteRunId===run.id))}
    async function loadEvaluationSuites(){setPanelState("evaluation-suites","loading","正在读取 Suite、Case 与汇总历史…");try{const[suites,runs,cases]=await Promise.all([request("/evaluations/suites?limit=500"),request("/evaluations/suite-runs?limit=500"),request("/evaluations/cases?limit=500")]);state.evaluationSuites=suites;state.evaluationSuiteRuns=runs;state.evaluationCases=cases;const selected=state.selectedSuite&&suites.find(item=>item.id===state.selectedSuite.id);state.selectedSuite=selected||suites[0]||null;renderSuiteDefinitions();if(state.selectedSuite)selectEvaluationSuite(state.selectedSuite.id);else{newEvaluationSuite();setPanelState("evaluation-suites","empty","还没有 Suite，请创建第一条。")}}catch(error){setPanelState("evaluation-suites","error",`Suite 工作台 加载失败：${error.message}`)}}
    async function saveEvaluationSuite(event){event.preventDefault();const button=byId("save-eval-suite");if(!state.suiteDraftCaseIds.length){setPanelState("evaluation-suites","error","Suite 至少需要一个 Case。");return}const payload={name:byId("eval-suite-name").value.trim(),case_ids:[...state.suiteDraftCaseIds]},path=state.selectedSuite?`/evaluations/suites/${encodeURIComponent(state.selectedSuite.id)}`:"/evaluations/suites",method=state.selectedSuite?"PUT":"POST";button.disabled=true;setPanelState("evaluation-suites","loading",state.selectedSuite?"正在更新 Suite…":"正在创建 Suite…");try{state.selectedSuite=await request(path,{method,headers:{"content-type":"application/json"},body:JSON.stringify(payload)});await loadEvaluationSuites()}catch(error){setPanelState("evaluation-suites","error",`保存失败：${error.message}`)}finally{button.disabled=false}}
    async function executeEvaluationSuite(){const button=byId("execute-eval-suite");if(!state.selectedSuite){setPanelState("evaluation-suites","error","请先选择或创建 Suite。");return}button.disabled=true;setPanelState("evaluation-suites","loading","正在按顺序执行独立 Case 会话…");try{const run=await request(`/evaluations/suites/${encodeURIComponent(state.selectedSuite.id)}/execute`,{method:"POST"});state.selectedSuiteRun=run;await Promise.all([loadEvaluationSuites(),loadSuiteOverview()]);selectEvaluationSuiteRun(run.id);setPanelState("evaluation-suites",run.passed?"ready":"error",`Suite 完成 · ${run.passed_count}/${run.total_count} 通过 · ${run.error_count} 错误`)}catch(error){setPanelState("evaluation-suites","error",`Suite 执行失败：${error.message}`)}finally{button.disabled=false}}
    async function loadSuiteOverview(){try{const runs=await request("/evaluations/suite-runs?limit=500");byId("metric-suite-runs").textContent=runs.length;byId("metric-suite-passed").textContent=runs.filter(run=>run.passed).length}catch(_error){byId("metric-suite-runs").textContent="!";byId("metric-suite-passed").textContent="!"}}
    function setupSuiteWorkbench(){const metrics=document.querySelector('[data-page="overview"] .metrics');[["Suite 运行数","metric-suite-runs"],["通过的 Suite","metric-suite-passed"]].forEach(([label,id])=>{const card=make("article","metric");card.append(make("span","",label));const value=make("strong","","—");value.id=id;card.append(value);metrics.append(card)});const toolbar=document.querySelector('[data-page="evaluations"] .section-head .toolbar'),suiteButton=make("button","action","Suite 管理");suiteButton.id="show-eval-suites";suiteButton.type="button";suiteButton.addEventListener("click",()=>{navigate("evaluation-suites");loadEvaluationSuites()});toolbar.prepend(suiteButton);byId("show-eval-cases").addEventListener("click",()=>navigate("evaluations"));byId("new-eval-suite").addEventListener("click",newEvaluationSuite);byId("refresh-eval-suites").addEventListener("click",loadEvaluationSuites);byId("eval-suite-name").addEventListener("input",event=>{state.suiteDraftName=event.target.value});byId("add-eval-suite-case").addEventListener("click",addSuiteCase);byId("eval-suite-form").addEventListener("submit",saveEvaluationSuite);byId("execute-eval-suite").addEventListener("click",executeEvaluationSuite);document.querySelector('[data-route="overview"]').addEventListener("click",loadSuiteOverview);loadSuiteOverview()}
    async function navigate(route){document.querySelectorAll("[data-page]").forEach(page=>{page.hidden=page.dataset.page!==route});document.querySelectorAll("[data-route]").forEach(button=>{if(button.dataset.route===route)button.setAttribute("aria-current","page");else button.removeAttribute("aria-current")});byId("page-title").textContent=titles[route];if(route==="overview")await loadOverview();if(route==="chat")await loadChat();if(route==="runs")await loadRuns();if(route==="evaluations")await loadEvaluations();if(route==="memories")await loadMemories();if(route==="library")await loadLibrary()}
    function graphStorageKey(sessionId){return `scholar-harness:graph-view:${sessionId}`}
    function restoreGraphView(sessionId){let saved=null;try{saved=JSON.parse(localStorage.getItem(graphStorageKey(sessionId))||"null")}catch(_error){}const valid=value=>Number.isFinite(value)&&Math.abs(value)<100000;const positions={};if(saved&&saved.positions&&typeof saved.positions==="object"){for(const [id,point] of Object.entries(saved.positions)){if(point&&valid(point.x)&&valid(point.y))positions[id]={x:point.x,y:point.y}}}const scale=valid(saved?.scale)?Math.max(.4,Math.min(2.5,saved.scale)):1;state.graphView={sessionId,positions,scale,panX:valid(saved?.panX)?saved.panX:0,panY:valid(saved?.panY)?saved.panY:0,needsFit:!saved,autoFit:saved?.autoFit!==false}}
    function saveGraphView(){const view=state.graphView;if(!view.sessionId)return;try{localStorage.setItem(graphStorageKey(view.sessionId),JSON.stringify({positions:view.positions,scale:view.scale,panX:view.panX,panY:view.panY,autoFit:view.autoFit}))}catch(_error){}}
    function updateGraphTransform(){const {canvas,zoomLabel}=state.graphControls,view=state.graphView;canvas.style.transform=`translate(${view.panX}px,${view.panY}px) scale(${view.scale})`;zoomLabel.textContent=`${Math.round(view.scale*100)}%`}
    function graphPosition(node,index,byNodeId){const stored=state.graphView.positions[node.id];if(stored)return stored;let depth=0,parent=node.parent_id;while(parent&&byNodeId.has(parent)){depth++;parent=byNodeId.get(parent).parent_id}return{x:20+depth*266,y:16+index*126}}
    function graphBounds(){const nodes=state.graph?.nodes||[],byId=new Map(nodes.map(node=>[node.id,node]));const points=nodes.map((node,index)=>graphPosition(node,index,byId));if(!points.length)return null;return{left:Math.min(...points.map(point=>point.x)),top:Math.min(...points.map(point=>point.y)),right:Math.max(...points.map(point=>point.x+224)),bottom:Math.max(...points.map(point=>point.y+102))}}
    function fitGraph(){const bounds=graphBounds();if(!bounds)return;const {scroll}=state.graphControls,view=state.graphView,padding=32;view.scale=Math.max(.4,Math.min(2.5,Math.min((scroll.clientWidth-padding*2)/(bounds.right-bounds.left),(scroll.clientHeight-padding*2)/(bounds.bottom-bounds.top))));view.panX=(scroll.clientWidth-(bounds.left+bounds.right)*view.scale)/2;view.panY=(scroll.clientHeight-(bounds.top+bounds.bottom)*view.scale)/2;view.needsFit=false;view.autoFit=true;updateGraphTransform();saveGraphView()}
    function zoomGraph(factor,clientX,clientY){const {scroll}=state.graphControls,view=state.graphView,rect=scroll.getBoundingClientRect(),x=clientX===undefined?rect.width/2:clientX-rect.left,y=clientY===undefined?rect.height/2:clientY-rect.top,next=Math.max(.4,Math.min(2.5,view.scale*factor));view.panX=x-(x-view.panX)*next/view.scale;view.panY=y-(y-view.panY)*next/view.scale;view.scale=next;view.needsFit=false;view.autoFit=false;updateGraphTransform();saveGraphView()}
    function onGraphWheel(event){if(!state.graph?.nodes.length)return;event.preventDefault();zoomGraph(Math.exp(-event.deltaY*.001),event.clientX,event.clientY)}
    function startGraphPan(event){if(event.button!==0||event.target.closest(".graph-node")||!state.graph?.nodes.length)return;const {scroll}=state.graphControls,view=state.graphView,startX=event.clientX,startY=event.clientY,panX=view.panX,panY=view.panY;scroll.setPointerCapture(event.pointerId);scroll.classList.add("panning");const move=next=>{if(next.pointerId!==event.pointerId)return;view.panX=panX+next.clientX-startX;view.panY=panY+next.clientY-startY;view.needsFit=false;view.autoFit=false;updateGraphTransform()};const finish=next=>{if(next.pointerId!==event.pointerId)return;scroll.removeEventListener("pointermove",move);scroll.removeEventListener("pointerup",finish);scroll.removeEventListener("pointercancel",finish);scroll.classList.remove("panning");saveGraphView()};scroll.addEventListener("pointermove",move);scroll.addEventListener("pointerup",finish);scroll.addEventListener("pointercancel",finish)}
    function resetGraphLayout(){state.graphView.positions={};renderTurnGraph();fitGraph()}
    function dragGraphNode(button,node,index,byNodeId,redrawEdges){button.addEventListener("pointerdown",event=>{if(event.button!==0)return;event.stopPropagation();const view=state.graphView,startX=event.clientX,startY=event.clientY,origin=graphPosition(node,index,byNodeId);let dragged=false;button.setPointerCapture(event.pointerId);const move=next=>{if(next.pointerId!==event.pointerId)return;const dx=next.clientX-startX,dy=next.clientY-startY;if(!dragged&&Math.hypot(dx,dy)<4)return;dragged=true;view.autoFit=false;button.classList.add("dragging");const point={x:origin.x+dx/view.scale,y:origin.y+dy/view.scale};view.positions[node.id]=point;button.style.left=`${point.x}px`;button.style.top=`${point.y}px`;redrawEdges()};const finish=next=>{if(next.pointerId!==event.pointerId)return;button.removeEventListener("pointermove",move);button.removeEventListener("pointerup",finish);button.removeEventListener("pointercancel",finish);button.classList.remove("dragging");if(dragged){button.dataset.ignoreClick="true";setTimeout(()=>{delete button.dataset.ignoreClick},0);saveGraphView()}};button.addEventListener("pointermove",move);button.addEventListener("pointerup",finish);button.addEventListener("pointercancel",finish)})}
    function setupGraphWorkbench(){const section=document.querySelector('[data-page="chat"]'),layout=section.querySelector(".chat-layout"),card=make("article","card"),top=make("div","row"),heading=make("h2","", "研究会话图"),runtime=make("span","pill","MiniPy · 非 Pi 原生会话"),caption=make("p","graph-caption","拖动节点调整位置，拖动空白处平移画布；滚轮缩放整个视图。点击节点只查看该路径，分支不会被删除。"),scroll=make("div","graph-scroll"),canvas=make("div","graph-canvas"),viewportActions=make("div","graph-viewport-actions"),zoomOut=make("button","action","−"),zoomLabel=make("span","graph-zoom","100%"),zoomIn=make("button","action","+"),fitButton=make("button","action","适配视图"),resetButton=make("button","action","重置布局"),actions=make("div","graph-actions"),continueButton=make("button","action primary","从此继续"),retryButton=make("button","action","重试此轮"),hint=make("span","muted","先选择一条回合"),inspector=make("div","graph-inspector");scroll.append(canvas);top.append(heading,runtime);[["缩小视图",zoomOut],["放大视图",zoomIn],["适配视图",fitButton],["重置节点布局",resetButton]].forEach(([label,button])=>{button.type="button";button.setAttribute("aria-label",label)});viewportActions.append(zoomOut,zoomLabel,zoomIn,fitButton,resetButton);[continueButton,retryButton].forEach(button=>{button.type="button";button.disabled=true});actions.append(continueButton,retryButton,hint);card.append(top,caption,viewportActions,scroll,actions,inspector);section.insertBefore(card,layout);state.graph=null;state.selectedTurnId=null;state.graphView={sessionId:null,positions:{},scale:1,panX:0,panY:0,needsFit:true,autoFit:true};state.graphControls={scroll,canvas,zoomLabel,continueButton,retryButton,hint,inspector};zoomOut.addEventListener("click",()=>zoomGraph(1/1.25));zoomIn.addEventListener("click",()=>zoomGraph(1.25));fitButton.addEventListener("click",fitGraph);resetButton.addEventListener("click",resetGraphLayout);scroll.addEventListener("pointerdown",startGraphPan);scroll.addEventListener("wheel",onGraphWheel,{passive:false});continueButton.addEventListener("click",()=>forkSelectedTurn(false));retryButton.addEventListener("click",()=>forkSelectedTurn(true));const forkSelect=byId("fork-entry");forkSelect.previousElementSibling.hidden=true;forkSelect.hidden=true;byId("fork-chat").hidden=true;byId("load-entries").hidden=true;const originalHandler=handleChatMessage;handleChatMessage=function(message){originalHandler(message);if(message.type==="session_ready"||message.type==="turn_complete"||message.type==="command_result"&&["fork","compact"].includes(message.command))loadTurnGraph(true)};document.querySelector('[data-route="chat"]').addEventListener("click",()=>{if(state.chat.session)loadTurnGraph()})}
    async function loadTurnGraph(selectActive=false){if(!state.chat.session)return;const sessionId=state.chat.session.id;try{const graph=await request(`/chat/sessions/${encodeURIComponent(sessionId)}/graph`);if(state.chat.session?.id!==sessionId)return;if(state.graphView.sessionId!==sessionId)restoreGraphView(sessionId);const previousCount=state.graphView.sessionId===sessionId?state.graph?.nodes.length:undefined;state.graph=graph;if(previousCount!==undefined&&previousCount!==graph.nodes.length&&state.graphView.autoFit)state.graphView.needsFit=true;const selected=graph.nodes.find(node=>node.id===state.selectedTurnId);if(selectActive||!selected)state.selectedTurnId=graph.nodes.filter(node=>node.active).at(-1)?.id||graph.nodes.at(-1)?.id||null;renderTurnGraph()}catch(error){state.graphControls.hint.textContent=`会话图加载失败：${error.message}`}}
    function renderTurnGraph(){
      const {canvas,continueButton,retryButton,hint,inspector}=state.graphControls,graph=state.graph;
      clear(canvas);clear(inspector);
      if(!graph||!graph.nodes.length){canvas.style.width="100%";canvas.style.height="100%";canvas.append(make("div","state","还没有回合。发送第一条研究任务后，这里会生成可分叉的会话图。"));continueButton.disabled=retryButton.disabled=true;hint.textContent="先发送一条任务";clear(byId("chat-log"));updateGraphTransform();return}
      const byNodeId=new Map(graph.nodes.map(node=>[node.id,node]));
      const positions=graph.nodes.map((node,index)=>graphPosition(node,index,byNodeId));
      const maxX=Math.max(...positions.map(point=>point.x+224)),maxY=Math.max(...positions.map(point=>point.y+102));
      canvas.style.width=`${Math.max(560,maxX+40)}px`;canvas.style.height=`${Math.max(220,maxY+40)}px`;
      const svg=document.createElementNS("http:"+"//www.w3.org/2000/svg","svg");
      svg.classList.add("graph-edges");svg.setAttribute("width","100%");svg.setAttribute("height","100%");canvas.append(svg);
      const redrawEdges=()=>{clear(svg);graph.nodes.forEach((node,index)=>{const parent=byNodeId.get(node.parent_id);if(!parent)return;const parentIndex=graph.nodes.indexOf(parent),from=graphPosition(parent,parentIndex,byNodeId),to=graphPosition(node,index,byNodeId),px=from.x+224,py=from.y+51,x=to.x,y=to.y+51,line=document.createElementNS("http:"+"//www.w3.org/2000/svg","path");line.setAttribute("d",`M ${px} ${py} C ${px+22} ${py}, ${x-22} ${y}, ${x} ${y}`);line.setAttribute("fill","none");line.setAttribute("stroke",node.active?"#c5f467":"#63847a");line.setAttribute("stroke-width","2");svg.append(line)})};
      graph.nodes.forEach((node,index)=>{const point=positions[index],button=make("button",`graph-node${node.active?" active":""}${node.id===state.selectedTurnId?" selected":""}`);button.type="button";button.style.left=`${point.x}px`;button.style.top=`${point.y}px`;button.append(make("strong","",`回合 ${index+1}${node.active?" · 当前路径":""}`),make("small","",node.prompt),make("small","",node.answer||"等待回复 / 从这里重新提问"));button.addEventListener("click",()=>{if(button.dataset.ignoreClick){delete button.dataset.ignoreClick;return}state.selectedTurnId=node.id;renderTurnGraph()});dragGraphNode(button,node,index,byNodeId,redrawEdges);canvas.append(button)});redrawEdges();updateGraphTransform();if(state.graphView.needsFit)fitGraph();
      const selected=byNodeId.get(state.selectedTurnId);continueButton.disabled=retryButton.disabled=!selected||state.chat.busy;hint.textContent=selected?`已选回合：${selected.prompt.slice(0,64)}${selected.continue_entry_id===graph.active_leaf_id?" · 可直接发送":" · 先选择继续或重试"}`:"先选择一条回合";chatStatus(state.chat.busy?"loading":"ready",selected?.continue_entry_id===graph.active_leaf_id?"当前分支可继续提问。":"选择历史回合仅查看；要提问请先点击“从此继续”或“重试此轮”。");const log=byId("chat-log");clear(log);if(!selected)return;selected.messages.forEach(message=>{if(message.content)log.append(make("div",`chat-message ${message.role}`,message.content))});inspector.append(make("h3","","本回合工具与证据"),make("p","muted",`创建时间：${time(selected.created_at)} · 工具结果：${selected.tools.length}`));selected.tools.forEach(tool=>{const item=make("article","tool-card");item.append(make("strong","",`${tool.name}${tool.is_error?" · 失败":""}`),make("pre","",tool.content));inspector.append(item)});if(!selected.tools.length)inspector.append(make("p","muted","这一轮没有工具结果；详细事件可在“运行追踪”中查看。"));if(selected.other_answers?.length){inspector.append(make("h3","","保留的旧回答（不参与当前上下文）"));selected.other_answers.forEach(answer=>inspector.append(make("pre","",answer)))}}
    function forkSelectedTurn(retry){const node=state.graph?.nodes.find(item=>item.id===state.selectedTurnId);if(!node||state.chat.busy)return;const anchor=retry?node.id:node.continue_entry_id;if(!anchor)return;sendChatCommand({type:"fork",entry_id:anchor});chatStatus("loading",retry?"正在从该问题重新开始…":"正在切换到该轮结束时的上下文…")}
    setupSuiteWorkbench();setupGraphWorkbench();
    document.querySelectorAll("[data-route]").forEach(button=>button.addEventListener("click",()=>navigate(button.dataset.route)));document.querySelectorAll("[data-go]").forEach(button=>button.addEventListener("click",()=>navigate(button.dataset.go)));byId("refresh-runs").addEventListener("click",loadRuns);byId("refresh-evaluations").addEventListener("click",loadEvaluations);byId("new-eval-case").addEventListener("click",newEvaluationCase);byId("eval-case-form").addEventListener("submit",saveEvaluationCase);byId("execute-eval-case").addEventListener("click",executeEvaluationCase);byId("run-evaluation").addEventListener("click",runEvaluation);byId("refresh-memories").addEventListener("click",loadMemories);byId("memory-filter").addEventListener("change",loadMemories);byId("refresh-library").addEventListener("click",loadLibrary);byId("search-form").addEventListener("submit",searchLibrary);byId("new-chat").addEventListener("click",newChat);byId("reconnect-chat").addEventListener("click",connectChat);byId("delete-chat").addEventListener("click",deleteChat);byId("chat-form").addEventListener("submit",submitChat);byId("abort-chat").addEventListener("click",()=>sendChatCommand({type:"abort"}));byId("compact-chat").addEventListener("click",()=>sendChatCommand({type:"compact",instructions:byId("compact-instructions").value}));byId("fork-chat").addEventListener("click",()=>{const entryId=byId("fork-entry").value;if(entryId)sendChatCommand({type:"fork",entry_id:entryId})});byId("load-entries").addEventListener("click",()=>sendChatCommand({type:"entries"}));request("/health").catch(()=>{byId("service-label").textContent="服务不可用"});chatStatus("empty","尚未连接会话");navigate("overview");
  </script>
</body></html>
"""
